[user]
    email = {{ .email }}
    name = Ingo Unterweger
{{- if .gitSigningKey }}
    signingKey = {{ .gitSigningKey }}

[commit]
    gpgsign = true
{{- end }}

[alias]
    ci = commit
    st = status

[pull]
    default = current
    followTags = true
    rebase = true

[fetch]
    prune = true
